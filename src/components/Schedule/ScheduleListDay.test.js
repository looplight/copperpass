import React 			from 'react';
import ScheduleListDay 	from './ScheduleListDay';

describe('<ScheduleListDay/>', () => {
	it('is not equal', () => {
		expect(1).not.toBe(2);
	});

	it('marks day as selected when clicked', () => {
		
	});

});